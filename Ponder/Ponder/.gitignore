# pre-tokenized data shards (too big for git — transfer separately)
*.bin

# training / runtime artifacts
final_outputs/
sweep_outputs/
wandb/
*.log
SWEEP_ID.txt
sweep_create.log

# caches
tmp/
.inductor_cache/
__pycache__/
*.pyc

# local scratch
_*.txt
